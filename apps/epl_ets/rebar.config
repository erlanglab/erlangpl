{erl_opts, [debug_info]}.
{deps, [{bear, ".*",
         {git, "git://github.com/boundary/bear.git", "master"}}
       ]
}.
%% Use with rebar3 to run proper test cases.
{plugins, [rebar3_proper]}.
{profiles,
    [{test, [
        {deps, [{proper, "1.1.1-beta"}]}
    ]}
]}.